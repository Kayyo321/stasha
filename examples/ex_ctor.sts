mod ex_ctor;

type Point: struct {
    ext i32 x;
    ext i32 y;
};

fn Point.new(stack i32 x, stack i32 y): Point {
    stack Point p;
    p.x = x;
    p.y = y;
    ret p;
}

ext fn main(void): i32 {
    stack Point p = Point.(3, 4);
    ret 0;
}
