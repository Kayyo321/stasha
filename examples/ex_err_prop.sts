mod ex_err_prop;

ext fn might_fail(i32 x): error {
    if (x < 0) { ret error.('negative'); }
    ret nil;
}

ext fn caller(i32 x): error {
    might_fail.?(x);
    ret nil;
}

ext fn main(void): i32 {
    ret 0;
}
