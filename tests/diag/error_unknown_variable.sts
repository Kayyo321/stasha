// Diagnostic test: undefined variable
// Expected: error pointing at 'foo' on line 9 with "did you mean 'for'?" suggestion
mod diag_unknown_variable;

int fn main(void): i32 {
    stack i32 x = 10;
    stack i32 y = 20;
    // 'foo' is not declared — should trigger "undefined variable" diagnostic
    stack i32 z = foo + y;
    ret z;
}
