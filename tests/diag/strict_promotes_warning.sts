// Diagnostic test: --strict promotes warnings to errors
// Expected: with --strict, unused variable warning becomes an error
// Feature: Phase 3B strict mode in diag_finish()
mod diag_strict_promotes;

ext fn main(void): i32 {
    stack i32 x = 10;
    stack i32 y = 20;
    ret x;
}
