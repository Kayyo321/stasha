// Diagnostic test: unused local variable warnings
// Expected: warnings for 'unused' and 'tmp', but NOT for '_ignored' or 'used'
mod diag_warn_unused;

ext fn main(void): i32 {
    stack i32 used = 5;
    stack i32 unused = 10;      // never read — should warn
    stack i32 _ignored = 99;    // _ prefix suppresses warning
    stack i32 tmp = used + 1;   // tmp itself never read — should warn
    ret used;
}
